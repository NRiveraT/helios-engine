# Shaders/Common

Shared Slang modules imported by other shaders.

V1 contents:
- `Bindless.slang` — `GetTexture2D(uint slot)`, `GetStorageBuffer(uint slot)`, `GetSampler(uint slot)`, `GetTLAS()` (Phase 5+)
- `Types.slang` — push-constant layouts, shared structs (Phase 6+)

Empty for now — populated as Phases 5/6 land.
