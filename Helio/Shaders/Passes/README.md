# Shaders/Passes

Verification graphics/compute shaders for V1. Game-side shaders live in `game/`, not here.

V1 contents:
- `Triangle.slang` — minimal vertex/fragment to prove the graphics path (Phase 6)
- `FullscreenBlit.slang` — fullscreen triangle that samples a texture (Phase 6)
