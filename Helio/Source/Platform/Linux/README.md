# Platform/Linux — placeholder

Not implemented in V1. Windows-only.

Future: SDL3 already runs on Linux, so most of `Platform/Windows/` would translate. File watcher would need `inotify` instead of `ReadDirectoryChangesW`.
