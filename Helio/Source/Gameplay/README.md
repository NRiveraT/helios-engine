# Gameplay — placeholder

Not in V1. V1 does **not** ship a gameplay framework (no `IGameMode`, no `Character`, no ability system). Your game's gameplay code lives in `game/Source/`, not here.

This module exists as a placeholder if you later choose to factor common gameplay primitives back into the engine.
