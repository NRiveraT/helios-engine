# Content/Defaults

Fallback assets generated procedurally at runtime — no file load needed.

V1 will provide: 1x1 white texture, 1x1 black texture, 1x1 flat-normal texture, 4x4 checkerboard. Used as fallbacks when a slot is unbound.
