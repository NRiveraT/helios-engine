# ThirdParty

All V1 dependencies come through vcpkg manifest mode (`vcpkg.json` at repo root). This directory is reserved for libraries that don't have a vcpkg port or need source-tree patches.

Currently empty.
